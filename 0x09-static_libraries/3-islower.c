#include "main.h"

/**
 * _islower - Checks if a character is lowercase.
 * @c: Argument of the function.
 * Return: 1 if c is a lowercase character, 0 if not.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
